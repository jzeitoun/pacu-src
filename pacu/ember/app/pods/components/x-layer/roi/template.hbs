{{#if hasBlock}}
  {{yield}}
{{else}}
  {{x-layer/roi/polygon attrs.roi.polygon
    active=attrs.roi.active
    busy=attrs.roi.busy
    error=attrs.roi.error
    invalidated=attrs.roi.invalidated
  }}
  {{#each attrs.roi.polygon as |pg|}}
    {{x-layer/roi/circle pg}}
  {{/each}}
{{/if}}
