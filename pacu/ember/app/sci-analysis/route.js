import Ember from 'ember';

export default Ember.Route.extend({
  model(param, transition) {
    // debugger
    // const record = Ember.get(this.modelFor('application'), 'sci-analysis-record');
    // if (Ember.isNone(record)) {
    //   transition.abort();
    // }
    // return record;
  }
});
