{{#each attrs.rois as |roi|}}
  {{svg-rect
    x1    = roi.x1
    x2    = roi.x2
    y1    = roi.y1
    y2    = roi.y2
    temp  = roi.temp
    doubleClick = (action "destroyROI" roi)
    onROIMoved = (action "refreshROI" roi)
  }}
{{/each}}
