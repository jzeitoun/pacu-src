import { fromTimestamp } from '../../../helpers/from-timestamp';
import { module, test } from 'qunit';

module('Unit | Helper | from timestamp');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = fromTimestamp(42);
  assert.ok(result);
});
