import { orIcon } from 'pacu/helpers/or-icon';
import { module, test } from 'qunit';

module('Unit | Helper | or icon');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = orIcon([42]);
  assert.ok(result);
});
