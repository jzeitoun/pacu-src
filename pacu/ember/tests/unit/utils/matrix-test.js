import matrix from 'pacu/utils/matrix';
import { module, test } from 'qunit';

module('Unit | Utility | matrix');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = matrix();
  assert.ok(result);
});
