import { ifEqThenClass } from 'pacu/helpers/if-eq-then-class';
import { module, test } from 'qunit';

module('Unit | Helper | if eq then class');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifEqThenClass([42]);
  assert.ok(result);
});
