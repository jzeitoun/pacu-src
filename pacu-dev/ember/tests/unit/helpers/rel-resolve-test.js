import { relResolve } from 'pacu/helpers/rel-resolve';
import { module, test } from 'qunit';

module('Unit | Helper | rel resolve');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = relResolve([42]);
  assert.ok(result);
});
